- impact — Change impact preview
aivocode lsp impact lsp/_daemon.py --symbol _is_running
Returns: all references + callers + callees, each with code_snippet. Answers "what will break if I change this?" in one call.
- explain
- read-symbol — Read exactly one symbol's body
- call-path from_func to_func — find the call chain between two functions
- class-tree class_name — full class hierarchy with inherited methods and overrides
