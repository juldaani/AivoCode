[project]
name = "aivocode"
version = "0.1.0"
requires-python = ">=3.12"

[project.scripts]
aivocode = "cli.main:main"

[tool.setuptools.packages.find]
include = ["cli*", "web_ops*", "lsp*", "file_watcher*", "api_server*"]

[tool.fastapi]
entrypoint = "api_server.app:app"
